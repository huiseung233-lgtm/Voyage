<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="2dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_day_root"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="1:1.2"
        app:cardCornerRadius="4dp"
        app:cardElevation="2dp"
        app:cardBackgroundColor="#FFF8E1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center">

            <TextView
                android:id="@+id/tv_day_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="15"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#3E2723"/>

            <TextView
                android:id="@+id/tv_day_score"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="80"
                android:textSize="10sp"
                android:textColor="#E65100"
                android:layout_marginTop="4dp"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <View
        android:id="@+id/view_today_indicator"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/ic_launcher_background"
        android:backgroundTint="#00000000"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/cv_day_root"
        app:layout_constraintBottom_toBottomOf="@id/cv_day_root"
        app:layout_constraintStart_toStartOf="@id/cv_day_root"
        app:layout_constraintEnd_toEndOf="@id/cv_day_root"/>

</androidx.constraintlayout.widget.ConstraintLayout>